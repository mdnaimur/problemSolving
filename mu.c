#include <stdio.h>

int main ()

{
    long long int n;
    while ( scanf("%lli",n)==1&&n!=0)

        {
        if (n<=100)
        printf ("f91(%lli)=91",n);
        else
        printf ("f91(%lli)=%lli,n,n-10");
        }
        return 0;
        }
