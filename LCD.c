//gusagu mirnoy
#include<stdio.h>
int main()
{
    int a,b,t,gcd;
    while(scanf("%d %d",&a,&b)!=EOF)
    {
        if(a==0) gcd=a;
         else if(b==0) gcd=b;
    }
printf("define %d %d %d ",a,b,gcd);
return 0;
}
